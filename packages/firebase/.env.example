# Firebase Functions Environment Variables
# Copy this file to .env.dev, .env.staging, or .env.prod and fill in values

# Environment name (development, staging, production)
LATITUDE_ENV=development

# Database name (dev, staging, or (default) for production)
DB_NAME=dev

# Twilio Configuration (for Bulk SMS feature)
# Get these values from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=your_account_sid_here
TWILIO_API_KEY_SID=your_api_key_sid_here
TWILIO_API_KEY_SECRET=your_api_key_secret_here
TWILIO_PHONE_NUMBER=77873

# Note: TWILIO_STATUS_CALLBACK_URL is no longer needed as an environment variable
# The callback URL is now automatically determined at runtime based on the environment