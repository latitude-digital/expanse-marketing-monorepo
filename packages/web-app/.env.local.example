# Firebase Emulator Configuration
# Copy this file to .env.local and adjust as needed

# Default behavior in development:
# - Functions emulator: ENABLED (using localhost:5001)
# - Firestore: PRODUCTION (using real data)
# - Auth: PRODUCTION (using real users)

# To enable ALL emulators:
VITE_USE_EMULATOR=true
VITE_USE_AUTH_EMULATOR=true
VITE_USE_FIRESTORE_EMULATOR=true
VITE_USE_FUNCTIONS_EMULATOR=true

# To disable Functions emulator (use production functions):
# VITE_USE_FUNCTIONS_EMULATOR=false

# To enable specific emulators:
# VITE_USE_AUTH_EMULATOR=true      # Use Auth emulator
# VITE_USE_FIRESTORE_EMULATOR=true # Use Firestore emulator