import React from 'react';
import { Outlet } from 'react-router-dom';
import './Surveys.scss';

function Surveys() {
  // logic to get the external survey goes here

  return (
    <Outlet />
  );
}

export default Surveys;
